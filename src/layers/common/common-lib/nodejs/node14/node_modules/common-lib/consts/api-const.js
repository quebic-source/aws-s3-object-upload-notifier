const APP_ID_PARAM = `appId`;
const APP_ID_HEADER_PARAM = `appid`;
const LOCATION_ID_PARAM = `locationId`;
const LOCATION_ID_HEADER_PARAM = `locationid`;
const VERSION_PARAM = `version`;
const APP_ID_PATH_PARAM = `{${APP_ID_PARAM}}`;
const APPS_RESOURCE_PATH = `/apps`;
const APPS_ID_RESOURCE_PATH = `${APPS_RESOURCE_PATH}/${APP_ID_PATH_PARAM}`;

const AUTH_APP_RESOURCE_PATH = `app`;
const AUTH_LOCATION_RESOURCE_PATH = `location`;

module.exports = {
    APP_ID_PARAM,
    APP_ID_HEADER_PARAM,
    VERSION_PARAM,
    APPS_RESOURCE_PATH,
    APPS_ID_RESOURCE_PATH,
    LOCATION_ID_PARAM,
    LOCATION_ID_HEADER_PARAM,
    AUTH_APP_RESOURCE_PATH,
    AUTH_LOCATION_RESOURCE_PATH
}
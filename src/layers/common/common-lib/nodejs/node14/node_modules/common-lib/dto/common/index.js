class GeneralMessage {
    constructor(message, code) {
        this._message = message;
        this._code = code;
    }

    toJSON() {
        return {
            message: this._message,
            code: this._code,
        }
    }
}

module.exports = {
    GeneralMessage
}